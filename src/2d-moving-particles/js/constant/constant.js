export const PARTICLE = {
    numParticles: 250,
    maxSpeed: 3,
    defaultLineWidth: 0.5
}

export const COLOR = {
    circle: 'rgba(255, 255, 255, 0.8)',
    line: 'rgba(255, 255, 255, 0.2)'
}

export const CIRCLE_RADIUS = {
    min: 1,
    max: 7
}

export const DISTANCE_COEFFICIENT = {
    toMouse: 0.1,
    toPoint: 0.02,
    increaseByRadius: 0.005,
    increaseByRadiusLineWidth: 0.3
}

export const CONFIG = {
    PARTICLE : PARTICLE,
    COLOR : COLOR,
    CIRCLE_RADIUS : CIRCLE_RADIUS,
    DISTANCE_COEFFICIENT : DISTANCE_COEFFICIENT
}
