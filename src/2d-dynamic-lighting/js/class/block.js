export default class Block {
    constructor(id, point, width, height) {
        this.id = id;
        this.point = point;
        this.width = width;
        this.height = height;
        this.visible = false;
    }
}
